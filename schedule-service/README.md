# Schedule Service

Manages customers, doctors, and consultation schedules for the Healthcare Scheduling System.
Every request requires a valid Bearer token, which is checked against the Auth Service. Exposes a
GraphQL API on port `3002`. See the [root README](../README.md) for how to run the full system and
example queries.
