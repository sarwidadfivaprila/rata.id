# Auth Service

Handles user registration, login, and token validation for the Healthcare Scheduling System.
Exposes a GraphQL API on port `3001`. See the [root README](../README.md) for how to run the full
system and example queries.
